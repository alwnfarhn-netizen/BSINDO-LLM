# BISINDO-LLM requirements.txt
# Adaptasi dari kevinjosethomas/sign-language-processing
#
# TETAP (sama dengan repo asli):
flask
flask-socketio
keras
tensorflow
tf-keras
opencv-python
pgvector==0.2.5
psycopg2-binary
python-dotenv
langchain_openai
transformers>=4.30.2
torch

# DIMODIFIKASI (ganti versi atau config):
# sentence-transformers versi multilingual (repo asli: ==2.2.2 hanya English)
sentence-transformers>=2.6.0

# DIHAPUS dari repo asli:
# - neuspell (spell checker English) → diganti indonlp

# DITAMBAH (baru untuk BISINDO-LLM):
# NLP Bahasa Indonesia
indobenchmark-toolbox     # IndoBERT utilities
indonlp                   # pipeline NLP BI (tokenisasi, spell check)
num2words[id]             # konversi angka ke kata Bahasa Indonesia

# Utilitas training
scikit-learn>=1.3.0       # LabelEncoder, train_test_split, metrics
numpy>=1.24.0
matplotlib>=3.7.0         # visualisasi training curves
seaborn>=0.12.0           # confusion matrix heatmap
tqdm                      # progress bar saat dataset processing

# Database migration
alembic                   # schema migration PostgreSQL
